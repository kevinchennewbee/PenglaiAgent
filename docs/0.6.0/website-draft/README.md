# 0.6.0 website draft (not live)

These HTML files are **source drafts**. Live `website/` and
https://penglai.pages.dev remain **0.5.12** until immutable `v0.6.0`
GitHub Release bytes exist.

Do not copy this directory onto `website/` or dispatch `deploy-website.yml`
from the release-prep PR. Publication copies EN then ZH after readback.
