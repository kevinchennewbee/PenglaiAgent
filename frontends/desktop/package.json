{
  "name": "penglai-desktop",
  "version": "0.3.6",
  "scripts": {
    "tauri": "tauri",
    "build": "tauri build",
    "build:mac": "tauri build --bundles dmg",
    "build:windows": "tauri build --bundles nsis"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
