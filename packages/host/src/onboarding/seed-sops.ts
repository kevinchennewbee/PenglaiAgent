/**
 * 种子 SOP 包（M3′ 冷启动：贾维斯第一天不空脑）。
 *
 * 从 0.3.x memory/ 精选的纪律类 SOP（吸收判断已点名 verify_sop /
 * penglai_compress_sop），按 0.4 公理适配后入包：
 *
 *   1. verify_sop —— 验证纪律。0.3 原文几乎逐字保留，唯一改动：
 *      「curl子资源确认非空壳」→「HTTP 抓取子资源确认非空壳」——原文
 *      过不了 0.4 审计 R2 外发禁令（\bcurl\b 命令模式），这正是「经蒸馏
 *      环审计规则器过一遍」的意义：种子包每条都跑 auditCandidateSop，
 *      过审才入树。
 *   2. penglai_compress_sop —— 可追溯压缩。核心原则逐字保留；记忆分层
 *      术语按 0.4 分区改写（0.3 的 L1-L4/ L4_raw_sessions → 0.4 的
 *      L1 指针 / 全局笔记 / SOP 技能树 / 项目记忆 / Evidence+checkpoint）。
 *
 * 名字与 0.3 文件名保持一致（verify_sop / penglai_compress_sop）：迁移
 * 工具遇到同名 SOP 时按「已在树中」处理，不会双份入树。
 */

export interface SeedSop {
  name: string;
  title: string;
  content: string;
}

export const SEED_SOPS: SeedSop[] = [
  {
    name: "verify_sop",
    title: "验证纪律（无工具证据的 PASS = SKIP）",
    content: `## 你的两个失败模式

1. **验证回避**：找理由不运行——读代码、描述"会怎样"、写PASS。读代码不是验证。
2. **被前80%迷惑**：看到通过的测试就想PASS，没注意一半功能是空壳。你的价值在最后20%。

owner 可能抽查重新执行你的命令——输出对不上，报告作废。

---

## 铁律（违反 → 结论无效）

1. **必须运行**。能跑的必须跑，能看的必须截图看。
2. **必须有工具证据**。无工具输出的 PASS = SKIP。
3. **独立验证**。实现者也是LLM——它的测试可能全是mock和happy path。测试套件是上下文，不是证据。

> **自检**：在写解释而不是调用工具？停。调用工具。

---

## 识别你的合理化借口

- "代码看起来是对的" → 运行它。
- "测试已经通过了" → 实现者是LLM。独立验证。
- "应该没问题" → "应该" ≠ "已验证"。运行它。
- "我没有浏览器/工具" → 你检查了可用工具吗？

---

## 验证动作（按产物类型，严格度∝风险）

| 产物类型 | 必做 |
|---|---|
| 网页/前端 | 打开+截图 → console错误 → HTTP 抓取子资源确认非空壳 |
| 脚本/CLI | 执行 → 检查stdout/stderr/exit code → 边界输入再跑 |
| 数据文件 | 格式校验 → 行数 → 抽查首/中/尾3条 |
| API/服务 | 调用endpoint → 响应形状(不只200) → 错误输入 |
| 配置/文档 | 完整读一遍内容 → 格式语法 → 未破坏已有 |
| Bug修复 | 复现原bug → 验证修复 → 回归测试 |
| 批量操作 | 总数 → 抽查首/中/尾 → 重复/遗漏 → 中间失败一致性 |

## 对抗性探测（至少运行一个，否则你只确认了happy path）

边界值(0/空/超长/unicode) · 幂等性(同一操作两次) · 缺失依赖 · 孤儿ID

---

## 发出结论前

**BEFORE PASS**：每步有命令输出？跑了对抗探测？独立验证了？
**BEFORE FAIL**：确认不是故意行为(查注释/文档)？不是已有防护覆盖？

---

## 输出格式

| # | 验证动作 | 工具 | 关键输出摘要 | PASS/FAIL |

每项检查：Command run → Output observed → Result
`,
  },
  {
    name: "penglai_compress_sop",
    title: "可追溯压缩 SOP（压缩/总结记忆必须留出处）",
    content: `# 可追溯压缩 SOP（压缩/总结记忆必须留出处）

触发场景：整理/压缩/归并全局笔记或项目记忆；把多条旧记录总结成一条；归档历史信息。
本 SOP 管**压缩内容时怎么不丢真相**（0.4 记忆分区：L1 指针 / 全局笔记 / SOP 技能树 / 项目记忆 .penglai/memory/）。

## 核心原则：压缩可以缩文字，不可以断来源
总结是有损的，错误会每轮复利。每条压缩后的结论必须能一步追溯回原文核对。

## 操作
1. **带出处指针**：压缩产物每条结论后缀来源，格式 \`(源:笔记名#关键词)\` 或
   \`(源:.penglai/memory/<文件>)\`。读者（未来的你）拿指针能直接定位原文。
2. **先立新、验证、再动旧**：禁止"总结+删原文"一步完成。顺序：写出压缩版（带出处）→
   复读复核压缩版没有歪曲 → 才能精简原文。改不动宁愿不改。
3. **拿不准就保留**：某条信息无法确认是否还有用 → 原样保留或仅迁移分区（项目记忆→全局归档笔记），不删。
4. **原始证据永不删**：任务证据轨（Evidence）与 checkpoint 是最后的真相来源，只读不动；压缩只发生在笔记层。

## 坑
- 把多条事实归并成一句"漂亮的概括"，丢了数字/路径/版本号 → 概括必须保留可执行细节。
- 出处写"之前的会话" → 等于没写，必须是能直接读取的具体路径或可搜索的关键词。
- 压缩时顺手"纠正"了看着不对的旧记录 → 未经行动验证禁改，存疑就标注 \`[待核]\` 留原文。
`,
  },
];
