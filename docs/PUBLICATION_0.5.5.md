# PenglaiAgent 0.5.5 publication contract

0.5.5 pins official DSH `0.1.1-rc.2` and three native targets. Publication is not part of this local development pass. Identity, source tests, and profile/plugin contracts must still name the exact future installers:

- `Penglai_0.5.5_macos_aarch64.dmg`
- `Penglai_0.5.5_macos_x64.dmg`
- `Penglai_0.5.5_windows_x64_setup.exe`

Do not create `v0.5.5`, `git push`, or `gh release create` until Owner later authorizes Codex acceptance and publication.
